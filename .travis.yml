language: python

python: 
  - "3.7"

before_install:
  - apt-get update
  - apt-get install -y /
    python3 / 
    python3-pip /
    python-virtualenv /
    python3-tk

install:
  - pip3 install -r requirements.txt

script:
  - python3 -m unittest

notifications:
  email: false